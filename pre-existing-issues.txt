`cargo fmt --manifest-path tools/game-data/Cargo.toml -- --check` reports existing formatting drift in `tools/game-data/src/editor.rs` and the `exploration.rs`, `gamble.rs`, `transfiguration.rs`, and `tutorial.rs` models. The reported sections predate this task and were not modified.

The `/opponent` debug tool uses a desktop two-column layout at a 390px viewport, producing 72px of horizontal document overflow. The deck panel begins at x=276px and ends at x=462px. This layout predates the opponent-algorithm selector removal and is outside this task's scope.

The Exploration template-assignment ledger initially contained a stale `exploration_sha256` even though all 1,042 action UUID and card UUID assignments matched generated Exploration exactly. The mechanic-coverage work repaired the hash before selecting its first redesign batch.

The Exploration provider's bulk-target eligibility test attempted to override Transfiguration eligibility on presentation data, but runtime eligibility is defined by the closed typed mechanic. The mechanic-coverage work repaired the fixture to use the existing Hastened mechanic whose production eligibility excludes fast Events.

`npm run audit:player-localization` exits nonzero on the repository's existing unclassified player-copy baseline, including journey debug and test fixtures. The Wave 0 Exploration purge message is localized and does not appear among the audit findings.

`npm run review` fails during the existing TypeScript test typecheck because `src/exploration/compound-action-plan.test.ts`, `multi-card-replacement-plan.test.ts`, `multi-card-transfiguration-plan.test.ts`, and `starter-card-transfiguration-plan.test.ts` import the absent `src/testing/journey-data-fixture` module and provide a `journeyData` property which is not part of `JourneyContent`. These test files and errors predate the Trox migration plan and are outside this documentation task.
