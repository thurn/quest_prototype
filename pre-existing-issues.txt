`scripts/dda-markdown.test.mjs` can exceed Vitest's 15-second default timeout
during `npm run review:full` under sustained repository-wide test load. The
test passes unchanged when run in isolation; its synchronous CLI subprocess
coverage is load-sensitive and unrelated to the localization migration.
