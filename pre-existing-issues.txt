`cargo fmt --manifest-path tools/game-data/Cargo.toml -- --check` reports existing formatting drift in `tools/game-data/src/editor.rs` and the `exploration.rs`, `gamble.rs`, and `transfiguration.rs` models. The reported sections predate this task and were not modified.
