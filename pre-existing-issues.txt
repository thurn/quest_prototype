Tutorial Editor: At a 1920x1080 desktop viewport, pointer activation of an
action's "Play tutorial from action N" button is intercepted by the underlying
tutorial-screen Cumulus layer, including after the button is scrolled into the
center of the editor. Programmatic button activation works, so the editor rail
appears to have a pre-existing stacking or hit-testing issue.

Cumulus Rules Text demo: At a 390px viewport, the Usage code example is clipped
horizontally instead of wrapping or exposing its full text.

Glossary Editor: Saving one entry serializes the complete glossary and rewrites
unrelated source formatting, including triple-quoted regex patterns and omitted
zero priorities. A one-field edit therefore creates unrelated TOML churn.

Glossary/Rules Text: The core test suite has three existing failures after
production glossary copy changes. `glossary.test.ts` hard-codes an older Points
definition, `card-text.test.ts` expects the lunar symbol token without its
glossary entry, and `RulesText.test.tsx` expects rendered definition copy not to
retain the ☪ character even though the current Exhaust Cost definition includes
it. These assertions depend on mutable production TOML copy or its exact parsed
shape.
