The default Vitest suite remains load-sensitive: on a heavily contended host, 15 tests across six Cumulus files failed after exceeding the global five-second timeout, and the first reveal-coordinator timeout caused overlapping React `act()` calls and cascading assertions; current master passed the same suite in GitHub Actions, so these tests should be made deterministic and independent of machine throughput.
