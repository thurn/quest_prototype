`scripts/regenerate-assets.sh` fails in its Trox extraction step with
`trox.literal-required` errors across canonical RON catalogs. The current Trox
extractor accepts the single-literal `Tx("...")` form but rejects structured
`Tx(text: "...", placeholders: {...})` values, including entries in
`data/atlas.ron`, `data/augury.ron`, `data/draft.ron`, `data/exploration.ron`,
`data/glossary.ron`, and `data/sites.ron`. Asset setup and the other generation
steps complete before extraction aborts.
