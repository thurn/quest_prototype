Pre-existing issue: lint failure in BattleInspector.test.tsx
===========================================================

Command: npm run lint (eslint src/)

Errors:
  src/battle/components/BattleInspector.test.tsx
    176:11  error  Unsafe assignment of an `any` value
            @typescript-eslint/no-unsafe-assignment
    176:37  error  Unsafe member access .edit on an `any` value
            @typescript-eslint/no-unsafe-member-access

Status: FAILS on master independent of this change.

Detail:
  Line 176 reads `const order: string[] = command.edit.order;`, where the
  `command` value is typed as `any`, so both the `.edit` member access and the
  assignment trip the typescript-eslint no-unsafe-* rules. The test file was
  introduced by commit c9869e60 ("feat(battle debug): add Shuffle deck button
  to inspector deck tools"), which predates the battle-UI fixes in this change
  and is untouched by them.

Suggested fix (out of scope here):
  Give `command` (or the dispatched command) a concrete type so `command.edit`
  and its `.order` field are properly typed, e.g. narrow it to the REORDER_DECK
  command shape before reading `command.edit.order`.
