Unresolved: `npm audit` reports `brace-expansion` 4.0.0–5.0.8 through
`node_modules/brace-expansion-core` as vulnerable to GHSA-rgw5-rvv9-x895. The
audit recommends upgrading that transitive package to 5.0.9.

Unresolved: `npm audit` also reports vulnerable transitive versions of
`fast-uri`, `hono`, `ip-address`, and `undici` (four package-level findings,
three high severity and one moderate). Fixed versions are available through
their dependency chains.

Unresolved: the standalone encounter editor has a fixed 1080px content width
at narrow viewports. At a 390px viewport, `document.documentElement.scrollWidth`
is 1080px and the encounter copy sits offscreen behind horizontal scrolling.

Unresolved: on the 390px Dream Augury comparison screen, the reveal coordinator
uses the horizontally scrolling offer row as its placement boundary. A touch-hold
InfoCard is therefore pinned to the row's top edge and overlaps the upper 39px of
its offer tile instead of clearing the complete source area.

Unresolved: the figment catalog has two inconsistent shapes. The authored TOML
and current battle-rules table contain 10 types, while the built-in fallback in
`src/battle/state/figment-catalog.ts` contains 14 types, including Ancient,
Enigma, Celestial, and Radiant. Journey documentation describes the fallback's
14-type shape, so available figments vary depending on catalog hydration.
