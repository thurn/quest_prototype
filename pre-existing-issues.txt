Full `npm test` has two stale Exhausted-description assertions:
`src/cumulus/components/card/GameCardReveal.test.tsx` and
`src/cumulus/screens/MobileBattleScreen.test.tsx` expect “cannot move,” while
the rendered glossary copy says “cannot challenge.” These tests couple their
expectations to mutable production glossary copy.
