Unity shop glass mesh serialization — fixed default-builder drift

- The committed `cumulus/Assets/CumulusMvp/Meshes/CumulusShopGlassPanel.asset`
  encoded a 0.02-unit depth, while the authoritative builder's default 2160px
  reference height produces a 0.018518518-unit depth from the radius-constrained
  geometry. A clean Unity EditMode run therefore modified the tracked mesh even
  though the glass-button change did not touch that geometry. The builder-canonical
  mesh output is included with the glass-button parity work so clean verification
  starts from the asset shape authored by `CumulusShopGlassDemoBuilder`.

## npm audit: OpenTelemetry baggage propagation advisory

`npm install` reports GHSA-8988-4f7v-96qf in the transitive
`@opentelemetry/core` dependency pulled in by `firebase-tools@15.23.0`.
The advisory is moderate severity (unbounded memory allocation while parsing
W3C Baggage headers). `npm audit` only offers `firebase-tools@14.23.0` as a
fix, which is a major-version downgrade and needs a separate dependency-policy
decision.

## npm audit: websocket-driver protocol advisories

`npm install` reports GHSA-mp7j-qc5w-4988 and GHSA-xv26-6w52-cph6 in the
transitive `websocket-driver` dependency. The advisories cover resource-limit
bypass through message compression and critical message corruption through
protocol length headers. `npm audit` reports that a fix is available; updating
the owning dependency chain needs a separate dependency-policy decision.

## Asymmetric battle preview ranks align to the left of the larger side

The `Fill 20 vs 9 + Voids` battle debug action sizes both combatants' rank
tracks from the player's ten-card back rank. The enemy's five-card back rank
and four-card front rank occupy the leading columns of those wider tracks, so
the enemy formation appears left-shifted while the twenty-card player
formation remains centered. The regular `Fill Battlefield + Voids` 9-vs-9
preview is centered.
## Dev wrapper can time out while `lsof` stats an Arq SMB mount

`npm run dev -- --port 5184` selected free emulator ports and started asset
setup, then its `lsof` readiness probe warned that a child process was hung
while statting `/Library/Application Support/ArqAgentUNC.noindex/...` and the
wrapper exited with `Timed out waiting for the Realtime Database emulator`.
The documented `npm run dev:vite -- --port 5184` path started successfully, so
Vite-only screen QA remained available. The wrapper should scope its port
inspection so unrelated slow or unavailable mounts cannot block startup.

## Coop battle construction drops opponent reconstruction logs

`createBattleInitProvider` passes a `deferOpponentLog` callback that discards
the deferred emitter from `createBattleInit`. As a result, event-sourced
`BEGIN_BATTLE` construction does not publish `opponent_signature_cards_selected`,
`opponent_deck_constructed`, or the corpus equivalents needed to reconstruct
how the opponent was generated. The provider comment previously claimed the
primary client emitted those records later, but no source consumer retains or
invokes the deferred emitter.

## Reloaded coop battle can render a stale phase after phase intents append

During browser QA on a reloaded `?goto=battle-playable&automation=0&game=...`
room, approving the existing AI end-turn proposal appended its gesture and
returned the fold to the player's Dreamwell phase. Pressing Next Phase then
appended a `SET_BATTLE_FLOW` intent for the player's Day phase to the emulator
room log, but the mounted battle surface continued to render Dawn. Additional
presses appended duplicate Day intents because the client kept computing the
target from its stale rendered phase. Reopening the same room reconstructed the
latest fold state. The room event log and the visible battle board should remain
in sync after every accepted intent so a stale client cannot emit duplicate
phase transitions.

## Production CSS contains a stray closing content tag

`npm run build` completes with an esbuild CSS syntax warning for an unexpected
`<` token. `src/battle/components/battle-start.css:561` contains the standalone
text `</content>`, which is not valid CSS and is emitted into the combined CSS
input passed to the minifier.

## npm audit: websocket-driver resource-limit and message-corruption advisories

`npm install` reports the critical GHSA-mp7j-qc5w-4988 and
GHSA-xv26-6w52-cph6 advisories in the transitive `websocket-driver@0.7.4`
dependency. `npm audit fix` reports that a non-breaking dependency update is
available; dependency-lock maintenance is outside the offer-tile UI change.

## A joined coop battle client can hide an open Foresee prompt

During battle drag QA, materializing card UUID
`647f5150-b2e0-424b-9480-27557642524e` parked its scripted Foresee prompt in
the shared fold. A second client joined the same room and rendered the live
battlefield without a Foresee overlay or prompt controls. Reposition intents
from that client reached the room event log but bounced with
`bounceReason: "prompt_pending"`. A client rendering a battle with an open
prompt should always expose the matching resolution surface so the shared fold
cannot appear playable while rejecting every new intent.
