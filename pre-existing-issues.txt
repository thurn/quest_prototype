Pre-existing issue (surfaced while documenting HoverZoomCard, Phase 4 Task 10):

src/tango/components/card/HoverZoomCard.tsx uses removed-state / "no longer"
documentation phrasing in its prop JSDoc and inline comments, which the
AGENTS.md documentation-style rule forbids ("describe the current system, not
what was removed"). This phrasing also flows verbatim into the generated
reference doc (.llms/skills/tango/components/hover-zoom-card.md, glossaryText
row). Specific spots:

- `glossaryText` prop JSDoc: "the same hover-help the old floating preview
  carried" (should describe the current behaviour without the "old ... preview"
  contrast).
- renderGlossaryStack JSDoc: "mirroring the hover-help the floating preview
  used to carry".

Fix: rewrite these to state the current behaviour directly (e.g. "shows the
glossary definitions for the card's gameplay terms beside the enlarged card")
without contrasting against the removed floating preview. Not fixed here
because the component source is outside this task's documentation scope.
