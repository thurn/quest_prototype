npm audit reports GHSA-2v37-7h3g-55p8 in the transitive development dependency nanoid (<3.3.17). Production dependencies are unaffected (`npm audit --omit=dev` reports zero vulnerabilities), and npm reports that an update is available.

`cargo fmt --manifest-path tools/game-data/Cargo.toml --check` reports pre-existing formatting drift in `tools/game-data/src/models/cards.rs:173` and `tools/game-data/src/models/figments.rs:378`; this Tutorial candidate does not modify either file.

`cargo fmt --manifest-path tools/game-data/Cargo.toml --check` also reports pre-existing formatting drift at the end of `tools/game-data/src/models/figments.rs`; the Sites activation does not modify that file.

`scripts/regenerate-assets.sh` updates `.llms/skills/cumulus/components/info-card.md` and `src/cumulus/metadata/cumulus-metadata.json` from the current source tree even though this Dreamwell activation does not change Cumulus component APIs, indicating that the tracked generated Cumulus references were stale on `master`.
