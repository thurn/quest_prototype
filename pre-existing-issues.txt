npm audit reports GHSA-2v37-7h3g-55p8 in the transitive development dependency nanoid (<3.3.17). Production dependencies are unaffected (`npm audit --omit=dev` reports zero vulnerabilities), and npm reports that an update is available.

`cargo fmt --manifest-path tools/game-data/Cargo.toml --check` reports pre-existing formatting drift in `tools/game-data/src/models/cards.rs:173` and `tools/game-data/src/models/figments.rs:378`; this Tutorial candidate does not modify either file.

`cargo fmt --manifest-path tools/game-data/Cargo.toml --check` also reports pre-existing formatting drift at the end of `tools/game-data/src/models/figments.rs`; the Sites activation does not modify that file.

The same Rust formatting check reports pre-existing trailing-whitespace drift at the end of `tools/game-data/src/models/figments.rs`; the Glossary activation does not modify that file.

`cargo fmt --manifest-path tools/game-data/Cargo.toml --check` reports additional pre-existing blank-line formatting drift at the end of `tools/game-data/src/models/glossary.rs`; the remaining CompatDocument conversion does not modify that file.

`scripts/regenerate-assets.sh` updates `.llms/skills/cumulus/components/info-card.md` and `src/cumulus/metadata/cumulus-metadata.json` from the current source tree even though this Dreamwell activation does not change Cumulus component APIs, indicating that the tracked generated Cumulus references were stale on `master`.

The Tides4 generator header and schema comments describe a signatureless Dream Avatar as drawing from the complete facet library recorded in `tidePoolByDreamAvatar`, while `src/draft/pool/variant-tides4.ts` instead chooses a random signatured Dream Avatar and borrows that Avatar's starter and facets. The generated artifact is internally consistent, but the comments do not describe how production consumes its signatureless entries.

`cargo test --locked --manifest-path tools/game-data/Cargo.toml` fails in `models::glossary::tests::production_catalog_preserves_reviewed_identity_contract` because the canonical glossary lowerer emits `rules-symbol` values that are absent from the checked-in compatibility TOML. This activation does not modify `data/glossary.ron`, `data/glossary.toml`, or `tools/game-data/src/models/glossary.rs`.
