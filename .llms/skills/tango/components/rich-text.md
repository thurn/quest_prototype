<!-- GENERATED by scripts/generate-tango-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via tango-metadata.json) and its demo
     entry in src/tango/docs/demos/rich-text.tsx. Regenerate: npm run tango-docs -->

# Rich Text

Components · Live demo & interactive props: `/tango#/rich-text`

Real consumers: **9** (imports outside `src/tango/docs/` and tests).

The design system's model for a run of formatted copy. The caller describes what the text is — plain prose, Dreamtides rules text with glossary-keyword emphasis and inline resource glyphs, a muted note, or a vertical stack of parts — and the renderer owns how it looks. Copy slots take a RichText, never an arbitrary node.

> **Guidance:** Build values with the `richText` constructors — `richText.plain`, `richText.rules`, `richText.note`, and `richText.stack` — and hand them to a copy slot (like `InfoCard.body`); reach for `RichTextView` only to render a standalone value inline.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `value` | `RichText` | yes | — |  |

### `value`: the `RichText` model

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `kind` | `"plain" \| "rules" \| "note" \| "stack"` | no |  |

## Usage

### Rules body in an InfoCard

Pass a `richText.rules(...)` value into a copy slot so glossary keywords gain the spark-amber emphasis and resource symbols render as inline glyphs.

```tsx
import { InfoCard } from "src/tango/components/overlay/InfoCard";
import { richText } from "src/tango/components/card/rich-text";

<InfoCard
  variant="text"
  title="Woodland Apparition"
  body={richText.rules("Support - Supported allies have +2 and unstoppable.")}
/>
```

### Blurb with a status note

Stack a plain blurb over a muted italic `note` so a site description and its status read as one value.

```tsx
import { richText } from "src/tango/components/card/rich-text";

const blurb = richText.stack(
  richText.plain("Spend essence on cards, dreamsigns, and services."),
  richText.note("Visited"),
);
```

### Render a standalone value

When copy is not already flowing through a slot, RichTextView renders a RichText value inline.

```tsx
import { RichTextView, richText } from "src/tango/components/card/rich-text";

<RichTextView value={richText.plain("A rising tide floods the board.")} />
```
