<!-- GENERATED by scripts/generate-tango-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via tango-metadata.json) and its demo
     entry in src/tango/docs/demos/card-stat-orb.tsx. Regenerate: npm run tango-docs -->

# Card Stat Orb

Components · Live demo & interactive props: `/tango#/card-stat-orb`

Real consumers: **2** (imports outside `src/tango/docs/` and tests).

The card-corner resource stat: a fitted white numeral over the energy, spark, or Dreamwell-energy glyph, with an optional monochrome transfiguration badge.

> **Guidance:** Changed numerals stay white. Use a down arrow for Empowered's reduced energy cost and an up arrow for Kindled's increased spark; the 30px design-size badge intersects the mark's lower-right edge and scales with it.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `variant` | `CardStatOrbVariant` = `"energy" \| "spark" \| "dreamwellEnergy"` | yes | — |  |
| `value` | `string` | yes | — | Displayed value (string so callers can pass `"X"` for variable cost). |
| `sizeVar` | `string` | yes | — | CSS length for the square orb diameter (e.g. `var(--cv-energy-orb-size)`). |
| `numberSizeVar` | `string` | yes | — | CSS length for the digit size — the single-digit ceiling, which doubles as the digit box edge (e.g. `var(--cv-energy-orb-font-size)`). Multi-digit values auto-shrink below this to fit. |
| `numberCapPx` | `number` | yes | — | Upper bound (px) for the digit auto-shrink search. The displayed size is the smaller of the CSS digit ceiling and the fitted size, so this only needs to sit at or above the rendered digit size. |
| `ariaLabel` | `string` | no | — |  |
| `tooltip` | `string` | no | — | Optional hover tooltip: a short plain-language description string. |
| `changeBadge` | `CardStatChangeBadge` = `"empowered" \| "kindled"` | no | — | Monochrome direction marker for a transfiguration-changed stat. Empowered uses a down arrow on energy cost; Kindled uses an up arrow on spark. |

## Usage

```tsx
import { CardStatOrb } from "src/tango/components/card/CardStatOrb";

<CardStatOrb
  variant="energy"
  value="2"
  sizeVar="44px"
  numberSizeVar="18px"
  numberCapPx={18}
  changeBadge="empowered"
/>
```
