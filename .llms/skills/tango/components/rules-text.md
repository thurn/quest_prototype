<!-- GENERATED by scripts/generate-tango-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via tango-metadata.json) and its demo
     entry in src/tango/docs/demos/rules-text.tsx. Regenerate: npm run tango-docs -->

# Rules Text

Components · Live demo & interactive props: `/tango#/rules-text`

Real consumers: **19** (imports outside `src/tango/docs/` and tests).

Renders Dreamtides rules copy from card data — resource pips, ability carets, and glossary keywords styled in place — so ability text reads the same everywhere it appears.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `text` | `string` | yes | — | The rules text to render. |

## Usage

Renders authored rules-text markup: energy / spark glyphs become inline pips, `▸` / `❖` markers become caret / bolt icons, and the curated keyword set is emphasized. Pass the card's rendered-text string as `text`.

```tsx
import { RulesText } from "src/tango/components/card/RulesText";

<RulesText text={"Support – Supported allies have +2✦ and unstoppable."} />
```
