<!-- GENERATED by scripts/generate-tango-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via tango-metadata.json) and its demo
     entry in src/tango/docs/demos/card-back.tsx. Regenerate: npm run tango-docs -->

# Card Back

Components · Live demo & interactive props: `/tango#/card-back`

Real consumers: **2** (imports outside `src/tango/docs/` and tests).

The canonical face-down Dreamtides card object: the shipped card-back sprite on the shared 5:7 card geometry, with fixed crop, edge, and elevation.

> **Guidance:** CardBack owns its appearance and takes only an accessible label plus an optional test id. Size and place it through a wrapper.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `label` | `string` | yes | — | Accessible description of this face-down card in its current zone. |
| `testId` | `string` | no | — | Optional stable test id for the face-down card object. |

## Usage

A face-down card in a named zone. The label is available to assistive technology and is not painted on the card.

```tsx
import { CardBack } from "src/tango/components/battle/CardBack";

<div style={{ width: 96 }}>
  <CardBack label="Face-down enemy card" />
</div>
```
