<!-- GENERATED by scripts/generate-cumulus-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via cumulus-metadata.json) and its demo
     entry in src/cumulus/docs/demos/avatar-portrait.tsx. Regenerate: npm run cumulus-docs -->

# Avatar Portrait

Characters & Collectibles · Live demo & interactive props: `/cumulus#/avatar-portrait`

Source-module importers: **12** (value imports outside docs and test/type fixtures; not a production-liveness measure).

The shared framed Avatar profile surface: a square `panel` crop for profile cards and popovers, or a close `thumb` crop for HUD rows and resident lists. Both render the transparent cutout over the canonical opaque portrait field and fall back to a monogram when art is unavailable.

> **Guidance:** The portrait always fills its caller-owned wrapper.

Put width, flex behavior, and placement on that wrapper; the component owns only its frame chrome and crop. Use Avatar Stage for full-body scene art.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `avatar` | `AvatarVisual` | yes | — | The avatar whose art and identity the portrait shows. |
| `variant` | `AvatarPortraitVariant` = `"panel" \| "thumb"` | no | — | Square framing: `panel` for profile surfaces or `thumb` for compact rows. Default `panel`. |
| `profile` | `{ id: AvatarId \| OpponentId; ability: LocalizedString; }` | no | — | Semantic Avatar profile represented by this portrait. Omit for decorative art. |
| `onPress` | `(() => void)` | no | — | Optional primary press action for selectable profile portraits. |
| `unavailable` | `boolean` | no | `false` | Keeps the profile readable while suppressing activation. |

### `avatar`: the `AvatarVisual` model

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `imageNumber` | `string` | no |  |
| `name` | `LocalizedString` | no |  |
| `title` | `LocalizedString` | yes |  |
| `portraitFocus` | `AvatarPortraitFocus` | yes | Normalized head position used to center subject-aware crops. |

## Usage

### Panel in a profile card

The square framing for profile cards and popovers.

```tsx
import { AvatarPortrait } from "src/cumulus/components/hud/AvatarPortrait";

<div style={{ width: 160 }}>
  <AvatarPortrait avatar={avatar} variant="panel" />
</div>
```

### Thumb in a HUD row

The close crop for compact rows and status objects.

```tsx
<div style={{ width: 56 }}>
  <AvatarPortrait avatar={avatar} variant="thumb" />
</div>
```
