<!-- GENERATED by scripts/generate-cumulus-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via cumulus-metadata.json) and its demo
     entry in src/cumulus/docs/demos/entity-reference.tsx. Regenerate: npm run cumulus-docs -->

# Entity Reference

Components · Live demo & interactive props: `/cumulus#/entity-reference`

Real consumers: **2** (imports outside `src/cumulus/docs/` and tests).

An inline, underlined card or Dreamsign name that reveals the canonical full entity on hover, keyboard focus, or touch hold through the shared coordinator.

> **Guidance:** Resolve the complete entity by UUID immediately before rendering. The component owns the displayed name and reveal model so duplicate names cannot affect identity; pass copies when prose names several identical card objects.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `entity` | `EntityReferenceModel` | yes | — | The UUID-backed card or Dreamsign named by the surrounding sentence. |
| `testId` | `string` | no | — | Optional stable selector for the semantic source. |

### `entity`: the `EntityReferenceModel` model

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `kind` | `"card" \| "dreamsign"` | no | Show the complete canonical card reading copy. Show the canonical Dreamsign object InfoCard. |

## Usage

### Card name in prose

Use the resolved UUID-backed card data; the inline source shares the same complete reading copy as GameCard.

```tsx
import { EntityReference } from "src/cumulus/components/card/EntityReference";

<p>
  Gain <EntityReference entity={{ kind: "card", card }} />.
</p>
```

### Dreamsign name in prose

Pass the resolved Dreamsign object to reuse its canonical object InfoCard and rules definitions.

```tsx
import { EntityReference } from "src/cumulus/components/card/EntityReference";

<p>
  Gain <EntityReference entity={{ kind: "dreamsign", dreamsign }} />.
</p>
```

### Repeated card objects

Pass the exact copy count when the sentence grants several identical cards; the reveal fans that many canonical card objects.

```tsx
import { EntityReference } from "src/cumulus/components/card/EntityReference";

<p>
  Gain 3 <EntityReference entity={{ kind: "card", card, copies: 3 }} /> cards.
</p>
```
