<!-- GENERATED by scripts/generate-cumulus-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via cumulus-metadata.json) and its demo
     entry in src/cumulus/docs/demos/card-browser-panel.tsx. Regenerate: npm run cumulus-docs -->

# Card Browser Panel

Card Workflows · Live demo & interactive props: `/cumulus#/card-browser-panel`

Real consumers: **4** (imports outside `src/cumulus/docs/` and tests).

The collection-browsing card surface: canonical responsive grid, optional search and sorting controls, and physical-card gestures in one scrolling panel.

> **Guidance:** Use this for reviewing or searching a card collection; its responsive geometry is component-owned.

The browser renders five columns on desktop and four on mobile, using the standard desktop card fit and compact mobile fit. Embedded browsers use floating glass, overlays become full-bleed on mobile, and full-screen hosts use the gallery scrim at every viewport.

Toolbar controls and drag, context-menu, and mobile double-tap gestures belong to this role. Transaction confirmation actions belong to Card Picker Panel.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `toolbar` | `CardBrowserToolbar` | no | — | Optional structured search, sort, and filter toolbar above the card grid. |
| `presentation` | `CardBrowserPresentation` = `"embedded" \| "overlay" \| "fullScreen"` | no | `embedded` | Host integration. `overlay` is floating on desktop and full-bleed on mobile. |
| `onCardPress` | `((entryId: EntryId) => void)` | no | — | Fires when an enabled card tile is activated. |
| `onCardDragStart` | `((entryId: EntryId, event: DragEvent<HTMLDivElement>) => void)` | no | — | Fires when a draggable card entry begins a native drag. |
| `onCardDragEnd` | `((entryId: EntryId, event: DragEvent<HTMLDivElement>) => void)` | no | — | Fires when a draggable card entry's native drag ends. |
| `onCardContextMenu` | `((entryId: EntryId, event: MouseEvent<HTMLDivElement, MouseEvent>) => void)` | no | — | Fires when a card entry requests its contextual actions. |
| `onCardDoubleTap` | `((entryId: EntryId) => void)` | no | — | Fires when a card receives two quick activations. While present, a primary card press waits briefly so a second tap can take precedence. |
| `title` | `LocalizedString` | yes | — | Header title, rendered as an `<h2>`. |
| `subtitle` | `LocalizedString` | no | — | Optional intro line under the title. |
| `rightAccessory` | `CardPanelAccessory` | no | — | Optional trailing header action. |
| `cards` | `readonly CardGalleryCardView<EntryId>[]` | yes | — | Resolved cards rendered in order. |
| `emptyLabel` | `LocalizedString` | no | — | Empty-state copy shown when `cards` is empty. |
| `testId` | `string` | no | — | Test id for the panel root. |

### `toolbar`: the `CardBrowserToolbar` model

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `segmented` | `CardBrowserSegmentedControl` | yes | Optional primary mode switch rendered on its own toolbar row. |
| `search` | `CardBrowserSearchControl` | yes | Search-by-name control. |
| `sort` | `CardBrowserSelectControl` | yes | Sort-order control. |
| `filter` | `CardBrowserSelectControl` | yes | Type-filter control. |

### `rightAccessory`: the `CardPanelAccessory` model

#### `GlassPanelGlassButtonAccessory`

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `kind` | `"glassButton"` | no |  |
| `button` | `GlassPanelGlassButtonProps` | no | Props forwarded to the labeled control; placement is panel-owned. |

#### `GlassPanelIconButtonAccessory`

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `kind` | `"iconButton"` | no |  |
| `button` | `GlassPanelIconButtonProps` | no | Props forwarded to the icon control; placement is panel-owned. |

## Usage

A searchable overlay browser with UUID-backed card entries.

```tsx
import { CardBrowserPanel } from "src/cumulus/components/card/CardBrowserPanel";

<CardBrowserPanel
  title={assertLocalized("Your Deck")}
  cards={cards}
  toolbar={toolbar}
  presentation="overlay"
  onCardContextMenu={openCardActions}
/>
```
