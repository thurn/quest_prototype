<!-- GENERATED by scripts/generate-cumulus-docs.mjs — do not edit by hand.
     Sources: the component's prop JSDoc (via cumulus-metadata.json) and its demo
     entry in src/cumulus/docs/demos/offer-tile.tsx. Regenerate: npm run cumulus-docs -->

# Offer Tile

Components · Live demo & interactive props: `/cumulus#/offer-tile`

Real consumers: **1** (imports outside `src/cumulus/docs/` and tests).

The 200×200 circular symbolic Dream Augury offer button: UUID-backed card art in height-preserving square frames, dreamsign art, and operation marks composed on a distinct glass surface without spelling out the complete offer.

> **Guidance:** Use the named offer kind that matches the category and pass every object surfaced by the generated offer. Fixed-target variants show their exact affected cards; chooser variants show every choice. Card-art frames widen to square at their established vertical scale, revealing more horizontal artwork. Four-card operations center their mark over the art grid, while smaller card sets pair the mark inline with enlarged art. Inner objects stay decorative, and the complete tile owns interaction plus its body-only InfoCard.

## Props

| Prop | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `model` | `OfferTileModel` | yes | — | The offer's strict symbolic view model. |
| `onPress` | `(offerId: string) => void` | yes | — | Activates the offer, reporting the stable `model.id`. |
| `testId` | `string` | no | `offer-tile` | Optional test selector; defaults to `offer-tile`. |

### `model`: the `OfferTileModel` model

| Field | Type | Optional | Description |
| --- | --- | --- | --- |
| `id` | `string` | no | Stable identity for this visible offer. Production callers should combine the encounter signature and offer id so simultaneous offers never collide. |
| `label` | `string` | no | Category name used by the tile's accessible button label. |
| `description` | `string` | no | Succinct action sentence rendered as the hover InfoCard's only copy. |
| `kind` | `"card-gift" \| "power-card" \| "card-draft" \| "copies-draft" \| "category-draft" \| "transfigured-draft" \| "card-bundle" \| "transfigure-card" \| "keyword-modification" \| "tribal-change" \| "transfigure-starters" \| "purge-card" \| "trade-card" \| "duplicate-card" \| "dreamsign-gift" \| "dreamsign-draft" \| "add-site"` | no |  |

## Usage

Build the model from UUID-backed offer objects. The category decides the symbolic layout; the caller supplies no layout, color, or nested-hover customization.

```tsx
import { OfferTile } from "src/cumulus/components/controls/OfferTile";

<OfferTile
  model={{
    id: encounterSignature + ":" + offerId,
    kind: "card-draft",
    label: "Card Draft",
    description: "Choose one of four cards to add to your deck.",
    cards: offeredCards.map(({ id, imageNumber }) => ({ cardId: id, imageNumber })),
  }}
  onPress={chooseOffer}
/>
```
