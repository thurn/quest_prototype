# Cumulus Sweep — 2026-07-18

**Date:** 2026-07-18  
**Audited range:** `9bdde28c4b40aad3149a5e7139e88abc1ea5ea30`..`ffbc62b778e66359ce955078ac065678d8dcd616`  
**Pre-sweep head:** `ffbc62b778e66359ce955078ac065678d8dcd616`

The range covers the UI-boundary convergence sequence: canonical card closure,
Dreamwell rendering, command menus, bootstrap and coop states, Pool Viewer,
retained diagnostics, battle overlays, and operator-tool ownership. Four
parallel evidence passes covered catalog adoption, doctrine, touched screens,
and token/demo drift. Each disposition below was reproduced at the pre-sweep
head before this sweep's fixes.

## Baseline Delta

| Baseline | Before | After |
| --- | ---: | ---: |
| `cumulus-duplicate-literals` `BASELINE` | 1 | 0 |
| `cumulus-orphan-tokens` `BASELINE` | 39 | 39 |
| `cumulus-ghost-components` `BASELINE` | 0 | 0 |
| `cumulus-css-press-scale` `BASELINE` | 0 | 0 |
| Outer TSX visual-rule baseline | 5 | 0 |
| Outer CSS visual-rule baseline | 5 | 0 |
| Pending Cumulus-presentation roles | 1 | 0 |
| Incubating entries with real consumers | 2 | 0 |

The orphan-token entries carry current complete-scale or production-bridge
contracts in `scripts/cumulus-orphan-tokens.test.mjs`; the stale-entry test
enforces that inventory.

## Verified Findings

- The outer role manifest classified `src/index.css` as pending presentation
  despite its application-entry responsibility. Its current app-shell role is
  constrained to reset, cursor defaults, and inherited color bridge; outer
  operator CSS remains explicitly tool-owned. The exact-count visual baselines
  are empty and the integrity test checks the current ownership model.
  Disposition: fixed in `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- Editor-facing tide glyphs bypassed the named glyph contract, and Dreamwell
  preview variables referenced names outside the Cumulus token vocabulary.
  Tide editor glyphs now render through `GlowIcon` with branded data glyphs;
  preview geometry uses its local constants and the canonical card blur token.
  Disposition: fixed in `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- `GlassDialog` and `MobileDeckViewer` carried the final duplicate-literal
  baseline. The dialog now reads the typed safe-area token, leaving the
  duplicate detector with an empty baseline. Disposition: fixed in
  `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- `CornerUtilityMenu` and `ContextActionMenu` each had two real consumers while
  their demos retained an incubating status. Their generated catalog entries
  now report the live adoption state. `CardTermDefinitions` remains incubating
  at zero consumers in its first zero-consumer sweep. Disposition: fixed and
  monitored.

- Bootstrap Firebase initialization failure rendered raw app markup. The
  front-door controller now selects `ApplicationStateScreen` with a
  `fatalConfiguration` model. Disposition: fixed in
  `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- Doctrine and generated prose carried stale component facts: TideDisc's
  self-owned reveal contract, DreamcallerPortrait's six framings,
  GlossaryDefinitionCard and CardOrderEditor's real consumer roles, InfoCard's
  liquid-glass presentation role, and glass-token ownership. Current source and
  generated references describe these contracts directly. Disposition: fixed
  in `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- Pool Viewer now has the direct `?goto=poolviewer` QA scene, registered in
  `src/runtime/qa-scenes.ts` and documented in the QA-scene table. The scene
  opens the shared overlay over a populated dreamscape. Disposition: fixed in
  `268f2231dd36d8938b15ae53d41b4e9e2f694fc7`.

- The one-consumer catalog inventory contains eleven narrow components, and
  five battle modules combine mutable state with retained overlay presentation.
  The lifecycle and composition decisions are recorded in
  `pre-existing-issues.txt`. Disposition: filed.

- MainMenuScreen uses a scene-local `zoom` around MainMenuButton, and Atlas CSS
  combines bridge-token roles with raw palette literals. Each needs a component
  or material-layer decision. Disposition: filed.

## QA And Reachability

The outer-role registry and boundary test enumerate every production TSX/CSS
file. The reachability review found no unclassified non-test UI module; the
catalog consumer inventory is regenerated with the Cumulus docs.

Cold visual review used the following direct scenes and captured artifacts:
`dreamcaller-select` (bootstrap), `dreamscape` (quest shell), `poolviewer`
(desktop and narrow overlay), the quest-shell Package Debug action (retained
diagnostics), and `battle-playable` (battle overlay suite). The review found no
material clipping, overlap, or captured render error. Artifacts are local QA
evidence and are not versioned.

## Recommended Program

Define catalog lifecycle policy for narrow components and split retained battle
overlay state adapters from pure Cumulus presentation. In the same program,
select the main-menu action hierarchy and define Atlas material-token ownership.
