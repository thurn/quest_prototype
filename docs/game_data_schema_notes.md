# Game-data schema notes

This reference preserves field-level authoring guidance for the canonical RON catalogs. Headings identify the compatibility field or table that each note describes; UUID annotations identify the records they document. Runtime and cross-catalog validation remains in the established TypeScript data compilers.

## `data/affiliations.ron`

### `AffiliationCatalog`

Affiliations: the thematic faction backing each non-starter dreamscape. An
affiliation's `tide_ids` are exactly three canonical Tide UUIDs. Their unweighted
sum is the affiliation vector used to select matching opponents and Dreamsigns.

Fields:
id UUIDv4 referenced by dreamscapes.
name Player-facing affiliation name.
atlas_card_theme Theme label used by Atlas generation.
tide_ids Three distinct Tide UUIDs defining the affiliation.

## `data/apollyon_incarnations.ron`

### `[[incarnations]]`

Apollyon incarnations: the final Avatar's ten guises. During Dreamscape
generation one incarnation is chosen to present Apollyon, swapping in that
guise's title and short deck description.

Fields:
id Lowercase RFC 4122 UUIDv4 identifier for the incarnation.
title Player-facing epithet shown as Apollyon's title.
description Short, surfaced description of the incarnation's deck.
deck_archetype
Internal deck-archetype label. Generated compatibility TOML
publishes this as `deck-type`; it is never shown in the UI.

## `data/atlas.ron`

Typed Dream Atlas rules and boss content. Layer roles, fill profiles, selection
policies, site composition, and the boss definition with its authored asset
references plus shared Atlas chrome are lowered by the `atlas_v1` adapter to
`data/atlas.toml`. Starter
Atlas descriptions belong to their Dreamscape records, while generic Atlas UI
copy is authored in the TypeScript view model. The runtime consumes the
validated `public/atlas-data.json` projection generated by
`scripts/setup-assets.mjs`.

## `data/augury.ron`

Typed Augury encounter composition, offer mechanics, tuning, and player-facing
offer copy. Each `AuguryArchetypeDefinition` uses a lowercase RFC 4122 UUIDv4 as its
canonical identity and pairs its presentation with one typed `ability` variant.
The ability variant determines the compatibility family, runtime id, selection
policy, and available quantity fields. The `augury_v1` adapter lowers the
catalog to generated `data/augury.toml`; runtime code consumes the validated
`public/augury-data.json` and `src/generated/config/augury-data.json` projections.

The catalog-level `encounter` record requires two offers from distinct families
and controls whether the player may decline both. Archetype weights are positive
relative lottery weights. Chooser sizes, granted copies, bundle bounds, starter
target limits, and presentation placeholders are validated by the typed model.
The Dreamsign and add-site presentations require `background_art: CardImage(...)`;
asset generation and `OfferTile` rendering consume that authored image number.

## `data/cards.ron`

Each card owns its stable UUID, rules text, gameplay shape, rarity, art, and any
closed gameplay `roles`. `StarterDeck` marks the ordered fixed journey deck and
must agree with `Starter` rarity. Exactly one `Special` card carries `Nightmare`.
Cards in ordinary selection pools use `Common`, `Uncommon`, `Rare`, or
`Legendary`; `Starter`, `Tutorial`, and `Special` are valid catalog rarities for
cards outside those pools.
The generated card-role projection resolves these UUIDs through the loaded card
catalog and supplies a fold hash for multiplayer configuration gating.

## `data/internal/internal_card_metadata.ron`

### `CardMetadataCatalog`

The catalog owns ordered `cards`, `tag_facets`, and `tide_facets` collections.
Each `CardMetadataDefinition` is routed by the card's canonical UUID and
supplies its numeric catalog identifier (`number`), Magic source name
(`mtg_origin`), and optional taxonomy labels (`tags`). The cards compiler joins
these fields into the generated `data/cards.toml` record with the same UUID.

### `tag_facets`

Ordered `FacetDefinition` values define card tags and their display colors.
The card editor's Manage Tags panel applies semantic edits to this collection.

### `tide_facets`

Ordered `FacetDefinition` values define card tides and their display colors.
The card editor's Manage Tides panel applies semantic edits to this collection.

The `internal_card_metadata_v1` adapter generates
`data/internal/internal_card_metadata.toml` for compatibility consumers.

## `data/draft.ron`

### `schema-version = 2`

Version of this file's schema. Increment only when the compiler contract changes.

### `cards-per-offer = 4`

Number of cards shown to the player in each draft offer.

### `picks-per-site = 5`

Number of cards the player picks before a Draft site is complete.

### `[[rarity-caps]]`

Optional per-rarity limits. Add another [[rarity-caps]] block for each rarity
that needs stricter limits than the strategy-wide copy cap.

### `rarity = "Legendary"`

Card rarity to which this block applies.

### `pool-copy-cap = 1`

Maximum copies of each card of this rarity placed in the generated pool.

### `max-picks-per-run = 1`

Maximum total cards of this rarity the player may pick during the entire run.

### `default-strategy = "tides4"`

Pool-generation strategy used by every journey.

### `deal-size = 150`

Total number of card copies tides4 deals into the generated draft pool.

### `copy-cap = 2`

Default maximum copies of any one card placed in the generated pool.
A matching [[rarity-caps]] block may lower this limit for a specific rarity.

### `max-facets = 3`

Maximum number of distinct facets tides4 may draw from when building the pool.

## `data/tides.ron`

The typed `TidesCatalog` owns the shared affinity space and the preconstructed
decks used by Tides4. Each `TideDefinition` has a UUID, localized display copy,
a localized grammatical package reference for Augury offers, resonance,
`Signature`, `Facet`, or `Neutral` kind, and a UUID-keyed card-count map. A card
contributes weight 2 to a Tide when its count is 2 and weight 1 when its count is
1.

`selection.band_fraction` and `selection.band_minimum` define the universal
seeded top-band sampler used after candidates are ranked. The compiler validates
all Tide and card references and lowers the catalog to `data/tides.toml`.

## `data/avatars.ron`

The canonical catalog is an ordered RON list of `AvatarDefinition` records.
Every record contains a lowercase UUIDv4 `id`, player-facing `name` and `title`,
one or more `ability_text` paragraphs, and a nested `portrait` with an integer
image number plus normalized `focus` coordinates. `starting_essence` is an
optional tuning override.

`signature_card_ids` is an ordered list of card UUIDs describing the avatar's
deck intent. The `tide_pool` record owns the optional signature Tide, the
available facet Tides, and neutral fill Tides used by Tides4 and opponent deck
construction. The compiler validates each reference against the Tide's declared
kind. The associated MTG archetype labels live in the internal
`data/internal/internal_avatar_metadata.ron` catalog, keyed by Avatar UUID.

The compiler validates unique Avatar IDs and portrait images, canonical
UUID formatting, non-empty display and ability fields, focus coordinates in
`[0, 1]`, unique signature references, and the internal metadata relationship.
It lowers the typed catalog to generated `data/avatars.toml` for runtime
consumers.

## `data/dream_guides.ron`

Dream Guides are the canonical identity and ownership catalog for every
non-starter dreamscape. Each guide owns one home dreamscape and one signature
site specialty. Runtime dreamscape guide and signature-site fields are derived
from these assignments.

Fields:
id Lowercase UUIDv4 identity for the guide.
name Player-facing guide name.
home_dreamscape_id Lowercase UUIDv4 foreign key for the guide's home.
portrait_source Source filename in the external dream-guide art catalog.
site_dialogue Ordered dialogue used by the guide's site presentation.
specialty Typed specialty variant defining the signature site and
its enhancement description. RandomSite and Gamble carry
their additional dialogue in the variant payload.

## `data/dreamscapes.ron`

Dreamscape definitions for the Dream Atlas. The source is an ordered list of
typed `DreamscapeDefinition` records. `data/dreamscapes.toml` is the generated
compatibility boundary consumed by the journey prototype.

Fields:
id Lowercase UUIDv4 identity referenced by authored catalogs.
name Player-facing dreamscape name.
art Scene and atlas-node asset references, each pairing a typed `AssetKey`
variant with its authoring filename.
kind Closed `Starter`, `Standard`, or `Boss` role variant.

`Starter` defines the signature site and ordered fixed-site sequence.
`Standard` defines one affiliation UUID and an ordered roster of three or four
Avatar UUIDs. `Boss` carries the Limbo identity and art while the Atlas
catalog owns its encounter configuration. Guide residency and signature sites
are derived from `data/dream_guides.ron`.

The Dreamscape editor publishes typed semantic operations against this RON
source. Name and affiliation edits patch one value span; resident changes patch
the affected roster spans in one revisioned transaction. Every save validates
the typed catalog and generated artifacts before atomic publication.

### `avatar-ids = [`

Spirit-animal payoffs: Grath rewards allied spirit animals directly; Radulf
and Demetrios are the go-wide character builders whose signature decks lean on
spirit-animal bodies (Starrunner, Sunshadow Eagle, Moonbound Wolf).

### `avatar-ids = [`

The Void: the deepest reclaim/void-matters pool. Seld Rakor, Seraveth, and
Vaela all key off cards in the void; Vethran's cost-X Discover rounds out the
region's grindy value plan. One of two four-avatar regions.

### `avatar-ids = [`

Tempo: proactive, fast-starting plans. Yveth Coravel bounces to reuse and
undercut; Drusus Calvus spends his opening turn pulling ahead on energy. Edran
is a deliberate, slightly unintuitive placement here — his outsider-figment
bodies give the Fjords a recurring tempo engine rather than a figment payoff.

### `avatar-ids = [`

Events: every resident is an event-cast payoff. Kasane copies events, Rael
foresees off a second event, and Ovanel refills events after playing one.

### `avatar-ids = [`

Characters: flood the board and reward each body. Korrax buffs an ally every
night, Kaleth awakens allies, and Senemhet turns a wide board's energy into
more characters.

### `avatar-ids = [`

Warriors: the whole warrior tribe lives here. Gunnar Deepforge copies the last
warrior played, Tensho discounts the first warrior drawn, and Valdren reclaims
warriors from the void.

### `avatar-ids = [`

Abandon: sacrifice your own allies for value. Ossian abandons to draw, Caedryn
abandons to materialize a bigger body, and Kragg discounts the next character
after abandoning.

### `avatar-ids = [`

Survivors: grind-it-out attrition in the wasteland. Kell Tarn's signature deck
is built on survivor bodies (Wreckland Maverick); Karev Soltis punishes the
opponent's scoring, and Calloway Flint is the scavenging raider admiral.

### `avatar-ids = [`

Figments: token-makers and figment payoffs. Serenath Veyl rewards
materializing a figment and Kael Voss discards to materialize a survivor
figment; Threxan fills the third slot, his turn-one card draw a clean fit for
the station's gamble-for-tempo identity.

### `avatar-ids = [`

Discard: turn discarding into advantage. Vrakmoth and Corvath both convert a
discard into a card or reclaim, Tessa loots (draw-then-discard), and Zeva
discards to reclaim an event. The second four-avatar region.

## `data/dreamsigns.ron`

### `DreamsignDefinition`

Each Dreamsign record owns its stable UUID, localized name and rules text,
art key, selection rarity, one to three Tide UUIDs, and editor tags. The
rarity is `Common`, `Uncommon`, `Rare`, or `Legendary`. Tide references place
the Dreamsign in the same affinity space used for cards, affiliations, Dream
Avatars, rewards, and opponent decks.

The `dreamsigns_v1` adapter generates `data/dreamsigns.toml`; setup-assets
publishes the browser catalog.

## `data/dreamsigns.tags.ron`

### `[[tags]]`

Tag registry for dreamsigns.toml.
Each [[tags]] entry defines an available dreamsign tag and its display color.
Managed by the dreamsign editor's "Manage tags" panel.

## `data/dreamwell.ron`

The typed `DreamwellCatalog` owns both the rules for constructing the shared
deck and the canonical card definitions placed into that deck.

### `rules.opening_orders: [0]`

Compatibility order groups included in full, once, at the front of the first
cycle. `Starting` cards lower to order 0.

### `rules.recurring_orders: [1, 2, 3, 4]`

Compatibility order groups sampled in this order in the first and every later
cycle. Tiers `One` through `Four` lower to orders 1 through 4.

### `rules.cards_per_recurring_order: 5`

Maximum cards sampled from each recurring order per cycle. A smaller group
contributes every available card.

### `rules.minimum_constructed_length: 62`

Complete cycles are appended until the prebuilt deck reaches at least this
length. A final complete cycle may exceed the threshold.

### `cards: [DreamwellCardDefinition(...)]`

### `DreamwellCardDefinition`

Dreamwell cards
---------------

Shared cards drawn one per turn during the Dreamwell phase (see
docs/battle_rules/battle_rules.md). Each card permanently raises the drawing
player's maximum ● by its `energy_added` value and may carry bonus-effect
paragraphs in `ability_text`, formatted per docs/cards2/style_guide.md.

Application-owned battle prompt copy and semantic prompt arguments are defined
in `src/data/dreamwell-prompts.ts`, keyed by stable Dreamwell card UUID.

`deck_tier` sets the card's position in the Dreamwell deck. `Starting` cards
appear only during the first cycle; tiers `One` through `Four` recur in
ascending order. `art` owns the image identity, provenance flag, and optional
pan/zoom crop. Compatibility-only card numbers are keyed by UUID in
`data/internal/internal_dreamwell_metadata.ron`.

The game-data compiler generates `data/dreamwell.toml` for runtime consumers.
The Dreamwell editor applies UUID-routed semantic operations to this RON source
and publishes the regenerated compatibility and runtime artifacts atomically.

## `data/economy.ron`

The authored source is a typed `EconomyCatalog`. Its `economy_v1` adapter
lowers typed fields and closed identities into `data/economy.toml`. The field
anchors below describe that generated compatibility document.

### `schema-version = 1` (generated)

Version of the economy authoring contract consumed by the asset compiler.

### `default-starting-essence = 200`

Essence granted at journey genesis when the selected Avatar has no
starting-essence override of its own.

### `dreamsign-cap = 12`

Maximum number of Dreamsigns a player may hold.

### `standard-card = 100`

Essence charged for a card in a regular Card Shop slot.

### `specialty-card = 200`

Essence charged for a card in a Specialty Shop slot.

### `dreamsign = 50`

Essence charged for a Dreamsign in a Dreamsign Bazaar slot.

### `card-slots = 5`

Number of card offers generated for each Card Shop visit.

### `dreamsign-slots = 0`

Number of Dreamsign offers generated for each Card Shop visit.

### `card-slots = 5`

Number of card offers generated for each Specialty Shop visit.

### `dreamsign-slots = 0`

Number of Dreamsign offers generated for each Specialty Shop visit.

### `card-slots = 0`

Number of card offers generated for each Dreamsign Bazaar visit.

### `dreamsign-slots = 3`

Number of Dreamsign offers generated for each Dreamsign Bazaar visit.

### `slot-counts = [`

Weighted distribution for how many generated shop slots receive a discount.
Each value is a slot count; weight is its relative sampling likelihood.

### `percentages = [`

Weighted distribution for the discount applied to each selected slot.
Each value is a percentage off the base price; weight is relative likelihood.

### `standard-price = 50`

Essence charged to reroll shop inventory at a standard site.

### `enhanced-price = 0`

Essence charged to reroll shop inventory at an enhanced site.

### `max-per-visit = 1`

Maximum number of inventory rerolls allowed during one shop visit.

### `standard = { min = 200, max = 300 }`

Inclusive Essence award range for a standard Essence site.

### `enhanced = { min = 400, max = 600 }`

Inclusive Essence award range for an enhanced Essence site.

### `fallback-essence = { min = 150, max = 350 }`

Inclusive Essence range used when a Reward site cannot offer a card reward.

### `standard-offer-count = 3`

Number of Dreamsign choices offered at a standard Revelation site.

### `enhanced-offer-count = 4`

Number of Dreamsign choices offered at an enhanced Revelation site.

### `marginal-costs = [40, 60, 90, 130, 180, 240]`

Essence price of each successive paid purge during one visit. The first entry
prices the first purge, and the number of entries is the paid-purge limit.

### `enhanced-discount-percent = 30`

Percentage removed from the summed purge price at an enhanced Purge site.

### `minimum-cost = 0`

Global lower and upper bounds applied to every final Essence quote.

### `step = 10`

Size of the discrete Essence increments used when rolling band jitter.

### `free-band = { base = 0, jitter = 0, floor = 0 }`

Cost band used by free transformations. In any band, base is the center
quote, jitter is the maximum seeded offset above or below base, and floor is
the band's lower bound before the global bounds are applied.

### `[[transfiguration.form-bands]]`

Cost bands selected directly by the named Transfiguration form. Each form
identity is code-owned; base, jitter, and floor use the meanings above.

### `[[transfiguration.stat-delta-bands]]`

Cost bands selected by the magnitude of a stat change. minimum-delta and
maximum-delta are inclusive; omitting maximum-delta makes the final band 4+.

### `base-essence = 100`

Essence awarded before completion-level scaling and reward modifiers.

### `essence-per-completion-level = 50`

Essence added for each completion level before reward modifiers.

### `minimum-essence = 0`

Lower bound applied to the final Essence reward after modifiers.

### `standard-wager = 50`

Essence committed when starting Three-Gate at a standard site.

### `enhanced-wager = 45`

Essence committed when starting Three-Gate at an enhanced site.

### `rewards = { six = 100, nine = 150, jack = 200 }`

Essence paid for reaching the code-owned Six, Nine, or Jack gate.

### `win-essence = 25`

Essence awarded for each winning Ladder Climb attempt.

### `attempts = [`

Cost schedule by one-based attempt number. Standard and enhanced costs are
the Essence committed before that attempt is played.

### `standard-wager = 30`

Essence committed when starting Starway Stairs at a standard site.

### `enhanced-wager = 20`

Essence committed when starting Starway Stairs at an enhanced site.

### `tiers = [`

Essence payout for cashing out at each one-based, code-owned reward tier.

### `standard-draw-price = 25`

Essence charged for each draw at a standard or enhanced Gamble site.

### `essence-reward = 100`

Essence granted when the drawn suit resolves to the Essence outcome.

### `default-essence-per-spark = 40`

Fallback Essence granted per spark by Exploration effects. An encounter's
essence-per-spark value in exploration.toml takes precedence when present.

## `data/exploration.ron`

### `schema-version = 1`

Schema version for the Exploration compiler and runtime parser.

### `actions-per-encounter = 2`

Two-choice screen contract: every authored encounter must present two actions.

### `[[encounter]]`

Exploration encounters are keyed by the UUID of the card whose artwork and
mechanics inspire the scene. Each encounter has exactly two player actions.
Each action contains its player-facing presentation and one typed gameplay
effect. Presentation slots reference runtime entities for display; the effect
variant defines selection, targeting, and resolution semantics.
selected prose: pair-2 (rank 2)
selected actions: pair-5 (rank 4)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 2)
selected actions: pair-1 (rank 2)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-3 (rank 3)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-2 (rank 2)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-2 (rank 1)
selected actions: pair-5 (rank 3)

### `[[encounter]]`

selected prose: pair-2 (rank 3)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-5 (rank 2)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-3 (rank 4)

### `[[encounter]]`

selected prose: pair-2 (rank 1)
selected actions: pair-5 (rank 3)

### `[[encounter]]`

selected prose: pair-3 (rank 1)
selected actions: pair-2 (rank 2)

### `[[encounter]]`

selected prose: pair-4 (rank 1)
selected actions: pair-1 (rank 3)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-3 (rank 4)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

### `[[encounter]]`

selected prose: pair-2 (rank 2)
selected actions: pair-4 (rank 1)

### `[[encounter]]`

selected prose: pair-3 (rank 1)
selected actions: pair-3 (rank 1)

### `[[encounter]]`

selected prose: pair-3 (rank 1)
selected actions: pair-1 (rank 2)

### `[[encounter]]`

selected prose: pair-4 (rank 1)
selected actions: pair-4 (rank 1)

### `[[encounter]]`

selected prose: pair-2 (rank 1)
selected actions: pair-2 (rank 1)

### `[[encounter]]`

selected prose: pair-1 (rank 1)
selected actions: pair-1 (rank 1)

## `data/figments.ron`

### `[[figments]]`

Figment catalog (rules §Figments). Each entry is one of the fixed figment
types a card effect can create. The figment editor (/figments) reads and
writes this file, and the battle UI sources a figment's name, character type,
spark, rules text, and art from the matching entry by `id`. Card effects and
runtime instances carry this UUID; `subtype` is a rules-facing character type
and is not a unique catalog key.

`keyword` is the type's implicit combat keyword (one of "vengeful",
"awakened", or "" for none) and is carried by every figment of the
type. `spark` is the base spark of a single figment.

## `data/battle.ron`

The typed `BattleRules` document defines the shared battle setup contract and
is composed into the generated opponent runtime data.

### `minimum_deck_size: 25`

Journey decks below this size are repeated until they meet the minimum.

### `player_opening_hand_size: 5`

Number of cards dealt to the player when a battle begins.

### `enemy_opening_hand_size: 5`

Number of cards dealt to the opponent when a battle begins.

### `score_targets: [10, 25]`

Score needed to win at each zero-indexed completion level. Later levels reuse
the final entry.

### `turn_limit: 50`

Maximum number of turns before the battle reaches its turn-limit outcome.

### `energy_cap: 10`

Maximum energy either side can accumulate during a battle.

### `hand_limit: 10`

Maximum cards held before basic automation discards excess cards.

### `starting_side: Player`

Side that takes the first turn. Valid values are `Player` and `Enemy`.

### `skip_player_opening_draw: true`

Whether the player omits their normal draw on their first turn.

### `opponent_signature_card_count: 3`

Number of opponent deck cards highlighted as Avatar signature cards.

## `data/opponents.ron`

The typed `OpponentsCatalog` document defines opponent progression and unified
deck construction. The compiler composes it with the battle,
Dreamwell, and internal AI catalogs for the generated opponent runtime data.

### `progression.ability_active_from_layer: 1`

All layer values are zero-indexed completion levels.
First layer where the opposing Avatar's ability is enabled.

### `progression.dreamsigns_from_layer: 3`

First layer where opponents may receive a Dreamsign.

### `progression.legendaries_from_layer: 5`

First layer where Legendary cards remain in opponent decks.

### `progression.starter_dilution: [10, 5]`

Starter cards inserted at each layer after cutting weak non-Starters. Missing
later entries mean that no Starter cards are inserted.

### `opponent_deck_size: 30`

Number of distinct cards in a mature generated opponent deck. Progression
replacements preserve this size.

## `data/internal/internal_ai.ron`

The typed `InternalAiCatalog` document defines the automated opponent's
UUID-keyed deck, evaluation model, response model, and planning presets.

### `journey_ai_deck: [DeckEntry(...)]`

Each `DeckEntry` adds one canonical card UUID and its positive copy count to the
AI deck. Card IDs must be unique here.

### `ai.journey_default_preset: Standard`

Preset ID used by AI-controlled opponents in ordinary journey battles.

### `ai.tutorial_default_preset: Standard`

Preset ID used by deterministic tutorial battle automation.

### `ai.evaluation.score_difference: 10.0`

Multiplier for the AI score minus the player score.

### `ai.evaluation.front_rank_spark: 1.0`

Multiplier for the AI-versus-player front-rank Spark difference.

### `ai.evaluation.back_rank_spark: 0.5`

Multiplier for the AI-versus-player back-rank Spark difference.

### `ai.evaluation.hand_card: 1.5`

Value assigned to each card currently in the AI's hand.

### `ai.evaluation.value_hint: 1.0`

Multiplier for card-specific strategic value hints supplied by AI card models.

### `ai.evaluation.energy_waste: 0.25`

Penalty applied to each point of AI energy left unspent in a candidate plan.

### `ai.evaluation.expected_points: 1.0`

Multiplier for estimated points the AI can score in the next Challenge.

### `ai.opponent_model.removal_prior: 0.1`

Per-card prior used to estimate whether the hidden opponent hand has removal.

### `ai.opponent_model.sample_safety_cap: 16`

Hard upper bound on opponent responses sampled for a candidate plan.

### `ai.opponent_model.response_archetype_priors.no_blocks: 1.0`

Relative sampling weights for modeled opponent responses. These need not sum
to 1; the model normalizes them when evaluating responses.
Weight for allowing all challengers through without assigning blockers.

### `ai.opponent_model.response_archetype_priors.block_biggest: 2.0`

Weight for blocking the challenger with the highest Spark first.

### `ai.opponent_model.response_archetype_priors.trade_evenly: 3.0`

Weight for seeking even trades between blockers and challengers.

### `ai.presets: { Standard: AiPreset(...) }`

Each map entry defines a selectable bundle of search settings keyed by its
typed stable identifier.

### `beam_width: 12`

Maximum candidate states retained at each breadth-search step.

### `opponent_mode: Expectiminimax`

Opponent-response aggregation: `Expectiminimax` uses the weighted mean;
`WorstCase` uses the lowest sampled result.

### `sample_count: 8`

Opponent responses requested per candidate, capped by sample-safety-cap.

### `search_depth: 16`

Maximum number of actions explored along one candidate plan.

### `journey_planning_budget_ms: 100`

Wall-clock planning budget for each journey AI decision.

### `tutorial_expansion_budget: 256`

Deterministic state-expansion budget for each tutorial AI decision.

## `data/sites.ron`

Typed `SitesCatalog` source for cross-site metadata and rules configuration.
Structural site and Gamble identifiers remain code-owned; this catalog supplies
the authored icons, glossary mappings, list-facing labels and titles, and tuning
for those fixed identities. Screen flow copy is authored by the corresponding
TypeScript view model. The Sites adapter emits the `schema-version = 1`
compatibility TOML consumed by asset generation.
Encounter sites are Augury and Exploration. Their shared
`encounter_sites.min_deck_for_purge` value gates purge-misfit targeting, while
`encounter_sites.placeable_sites` defines which sites their rewards may offer
for placement in the current Dreamscape.

## `data/tutorial.ron`

Typed `TutorialCatalog` source for the complete standalone tutorial scenario.
The `tutorial_v1` adapter emits the camel-case compatibility TOML consumed by
the runtime asset pipeline.

### `journey_guidance`

Persistent guidance for journey surfaces and the first two battles.

### `battle.forced_player_draws`

`battle.forced_player_draws` and `battle.forced_enemy_draws` are drawn in order
after the authored tutorial actions hand control to the playable battle.

### `battle.dreamwell_draws`

This is the complete shared Dreamwell deck prefix, including the two cards
drawn by authored actions before the playable battle begins.

### `actions` and `triggers`

Ordered typed variants keyed by UUIDv4 identities. The adapter maps reviewed
legacy identities at the generated compatibility boundary.
